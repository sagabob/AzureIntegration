<policies>
    <inbound>
        <base />
        <set-header name="Accept-Encoding" exists-action="override">
            <value>identity</value>
        </set-header>
        <set-header name="Origin" exists-action="delete" />
        <set-header name="Referer" exists-action="delete" />
        <set-header name="X-Forwarded-Host" exists-action="delete" />
        <set-header name="Forwarded" exists-action="delete" />
        <set-header name="Host" exists-action="override">
            <value>@(new Uri(context.Api.ServiceUrl.ToString()).Host)</value>
        </set-header>
        <set-backend-service base-url="@(context.Api.ServiceUrl.ToString().TrimEnd('/'))" />
        <rewrite-uri template="@{
            var suffix = context.Api.Path ?? string.Empty;
            var prefix = suffix.StartsWith('/') ? suffix : '/' + suffix;
            var path = context.Request.OriginalUrl.Path;
            if (path.StartsWith(prefix, StringComparison.OrdinalIgnoreCase))
            {
                path = path.Substring(prefix.Length);
            }
            return string.IsNullOrEmpty(path) ? '/' : path;
        }" />
        <choose>
            <when condition='@(context.Request.Headers.GetValueOrDefault("Authorization", "").StartsWith("Bearer ") == false)'>
                <return-response>
                    <set-status code="401" reason="Unauthorized" />
                    <set-header name="Content-Type" exists-action="override">
                        <value>application/json</value>
                    </set-header>
                    <set-body>{"error":"Authorization Bearer token is required"}</set-body>
                </return-response>
            </when>
            <when condition='@(string.IsNullOrWhiteSpace(context.Request.Headers.GetValueOrDefault("X-Access-Token", "")))'>
                <return-response>
                    <set-status code="401" reason="Unauthorized" />
                    <set-header name="Content-Type" exists-action="override">
                        <value>application/json</value>
                    </set-header>
                    <set-body>{"error":"X-Access-Token is required"}</set-body>
                </return-response>
            </when>
        </choose>
    </inbound>
    <backend>
        <forward-request timeout="120" buffer-request-body="true" buffer-response="true" />
    </backend>
    <outbound>
        <base />
        <set-header name="X-Gis-Upstream" exists-action="override">
            <value>@(context.Request.Url.ToString())</value>
        </set-header>
        <set-body>@{
            return context.Response.Body.As<string>(preserveContent: true);
        }</set-body>
    </outbound>
    <on-error>
        <base />
    </on-error>
</policies>
